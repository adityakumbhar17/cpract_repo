//write the program to add two numbers
#include <stdio.h>
int main() {
    int a, b, c; //variables to store the input numbers and the result 
    int d; //variable to store the difference of a and b
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b); //sanning the input numbers from the user
    c=a+b; // Calculate the sum of a and b
    d=a-b; // Calculate the difference of a and b;
    printf("Sum = %d\n", c); // Output the result hii hello 
    printf("Difference = %d\n", d); // Output the difference
    pfrintf("Hello World!"); // Output Hello World
    return 0; 
}